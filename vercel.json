{
  "buildCommand": "prisma generate && prisma migrate deploy && next build && npx prisma db seed",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["dub1"],
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "false"
  }
}
